/* #include<stdio.h>  // 2025.09.13. 복습 합, 차, 곱, 몫, 나머지

int main(void) {

    int a, b;
    printf("두 정수를 입력하세요. :");
    scanf("%d %d", &a, &b);

    printf("합 : %d\n", a + b);
    printf("차 : %d\n", a - b);
    printf("곱 : %d\n", a * b);
    printf("몫 : %d\n", a / b);
    printf("나머지 : %d\n", a % b);

    return 0;
}
*/

/*#include<stdio.h> // 2025.09.13. 복습 5개 정수를 받아서 제일 큰 값과 제일 작은 값 구하기.

int main(void) {
    int a, b, c, d, e;
    printf("5개의 정수를 입력하세요. :");
    scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);

    int max = a;
    int min = a;

    if(max < a) max = a;
    if(min > a) min = a;
    if(max < b) max = b;
    if(min > b) min = b;
    if(max < c) max = c;
    if(min > c) min = c;
    if(max < d) max = d;
    if(min > d) min = d;
    if(max < e) max = e;
    if(min > e) min = e;

    printf("제일 큰 수 : %d\n", max);
    printf("제일 작은 수 : %d\n", min);

    return 0;
} */
